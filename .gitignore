# =============================================================================
# de_template: .gitignore
# =============================================================================

# --- Active environment file (generated via: make env-select ENV=env/<profile>.env) ---
# env/ profiles ARE committed — they are templates, not secrets.
.env

# --- Build outputs (generated by: make build-sql) ---
build/

# --- Benchmark results ---
benchmark_results/

# --- Python / uv ---
# uv.lock IS committed (reproducible installs) — do NOT add it here
__pycache__/
*.py[cod]
*.pyo
.pytest_cache/
.venv/
venv/
env.bak/
.coverage
htmlcov/
.mypy_cache/
.ruff_cache/
*.egg-info/
dist/
*.log

# --- dbt generated files ---
dbt/target/
dbt/dbt_packages/
dbt/logs/
dbt/.user.yml
dbt/*.duckdb
*.duckdb

# --- OS / IDE ---
.DS_Store
Thumbs.db
desktop.ini
.vscode/
.idea/
*.swp
*.swo

# --- Docker local state ---
**/.docker/

# --- Data files (user-provided; mount via HOST_DATA_DIR) ---
data/
*.parquet

archive/
